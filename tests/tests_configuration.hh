#pragma once
#include <cstddef>

namespace linalgwrap {
namespace tests {

struct tests_constants {
    /** The maximal matrix size arbitrary matrices have */
    static constexpr std::size_t max_matrix_size = 10;
};
}
}
