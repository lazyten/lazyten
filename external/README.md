The external dependencies are checked out into this directory if required.

Configuring and building of this directory is invoked by the cmake script
cmake/setup_dependencies.cmake via add_subdirectory( )
